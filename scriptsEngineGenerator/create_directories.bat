@echo off
echo === Generating folders for Window engine ===
if not exist PulseEngineBuild mkdir PulseEngineBuild
if not exist "PulseEngineBuild/Logs" mkdir "PulseEngineBuild/Logs"
if not exist "PulseEngineBuild/PulseEngineEditor" mkdir "PulseEngineBuild/PulseEngineEditor"
